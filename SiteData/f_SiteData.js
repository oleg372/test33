$(document).ready(function()
{
/*
 var items = [];
 $('a.js-zoom-img').each(function (index, el)
   {
    items.push({ 
      src: $(el).attr('href'), 
      w: $(el).attr('data-w'), 
      h: $(el).attr('data-h'),
      title: $(el).attr('title')
    });

    $(el).attr('href', 'javascript:;').attr('data-i', index);
   });

 // <a href="tovx-505-5646.jpg" class="js-zoom-img" title="" data-w="1000" data-h="1000"><img src="tovb-505-5646.jpg"></a>
 
 $('a.js-zoom-img').click(function ()
   {
    var pswpElement = document.querySelectorAll('.pswp')[0];
  
    // define options (if needed)
    var options = {
       index: parseInt($(this).attr('data-i')) // start at first slide
      ,shareEl: false
      ,showHideOpacity: true
      ,bgOpacity: 0.85
    };
 
    // Initializes and opens PhotoSwipe
    var gallery = new PhotoSwipe(pswpElement, PhotoSwipeUI_Default, items, options);
    gallery.init();
    return false;
   });
*/
});

